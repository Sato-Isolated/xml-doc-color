namespace XmlDocColorFixture;

/// <summary>
/// Builds a <see cref="Result"/> from <paramref name="source"/> &amp; validates it.
/// <![CDATA[value < limit]]>
/// </summary>
/// <param name="source">Input value.</param>
public sealed class Example
{
    public string Build(string source) => source;
}

public sealed class Result;
